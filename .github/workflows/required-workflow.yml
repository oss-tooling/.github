name: Example Required Workflow
on:
  pull_request:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    name: policy-enforce-pr
    steps:
      - name: Run a one-line script
        run: echo Hello, world!
    
